# Minimal set of dependencies. Versions left unpinned; pin if you need reproducibility.

langchain                 # create_agent, langchain.tools, langchain.messages, AgentState
langchain-openai          # ChatOpenAI -> OpenRouter (base_url + OPENROUTER_API_KEY)
langgraph                 # @task/@entrypoint, InMemorySaver, interrupt, Command, RetryPolicy
langchain-text-splitters  # RecursiveCharacterTextSplitter
langchain-huggingface     # HuggingFaceEmbeddings
sentence-transformers     # backs the HF embeddings (pulls torch) — needed for a real embed step
numpy                     # required by InMemoryVectorStore similarity search
# langchain-core (InMemoryVectorStore, Document) and langsmith (tracing) install transitively with langchain.
